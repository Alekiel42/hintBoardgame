extends partials/layout.pug 
include mixins/tiles.pug

block append content 
    div(class="content-center")
        if hints 
            each hint in hints 
                +hints(hint)
        else 
            div=message


