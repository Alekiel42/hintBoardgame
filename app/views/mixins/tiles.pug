mixin hints(hint)
    if hint.content 
        div(class="tiles__item")
            p= hint.content 
    if hint.picture 
        div(class="tiles__item")
            img(src='/images/tiles/' + hint.picture)
    if hint.sound 
        div(class="tiles__item")
            audio(preload="auto", autobuffer, controls)
                source(src='/sounds/' + hint.sound)

